# MemoryOps frontend configuration
# Copy to .env.local for local development.

# Must match a workspace row in the backend database.
VITE_MEMORYOPS_WORKSPACE_ID=00000000-0000-7000-8000-000000000001

# API keys are intentionally not persisted by the frontend.