services:
  postgres:
    image: postgres:16
    container_name: memoryops-postgres
    environment:
      POSTGRES_DB: memoryops
      POSTGRES_USER: memoryops
      POSTGRES_PASSWORD: memoryops
    # Bind to localhost only — dev convenience; use docker-compose.prod.yml for production
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U memoryops -d memoryops"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:7
    container_name: memoryops-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  qdrant:
    # Pin to a specific release — do not use :latest in shared environments
    image: qdrant/qdrant:v1.13.6
    container_name: memoryops-qdrant
    ports:
      - "127.0.0.1:6333:6333"
      - "127.0.0.1:6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    healthcheck:
      test: ["CMD", "bash", "-c", "</dev/tcp/127.0.0.1/6333"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  frontend:
    image: quazmoz/memoryops:frontend-latest
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api
        # VITE_MEMORYOPS_WORKSPACE_ID is no longer baked into the image.
        # Set MEMORYOPS_WORKSPACE_ID at runtime instead.
    container_name: memoryops-frontend
    ports:
      - "127.0.0.1:5173:8080"
    environment:
      # Runtime workspace ID served by /config.json
      MEMORYOPS_WORKSPACE_ID: ${MEMORYOPS_WORKSPACE_ID:-${VITE_MEMORYOPS_WORKSPACE_ID:-}}
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/memoryops-frontend", "--healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  api:
    image: quazmoz/memoryops:api-latest
    build:
      context: .
      target: api-runtime
    command: ["/usr/local/bin/api"]
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      DATABASE_URL: postgres://memoryops:memoryops@postgres:5432/memoryops
      REDIS_URL: redis://redis:6379
      QDRANT_URL: http://qdrant:6334
      APP_HOST: 0.0.0.0
      APP_PORT: "8080"
      APP_ENV: ${APP_ENV:-development}
      APP_SECRET_KEY: ${APP_SECRET_KEY}
      # Webhook signing secrets are stored per workspace via /v1/workspaces/{id}/integrations.
      WORKSPACE_CREATION_SECRET: ${WORKSPACE_CREATION_SECRET}
      # Set to false after bootstrap to disable POST /v1/workspaces.
      WORKSPACE_CREATION_ENABLED: ${WORKSPACE_CREATION_ENABLED:-true}
      # Trusted reverse-proxy CIDRs for X-Forwarded-For (comma-separated CIDR notation)
      TRUSTED_PROXY_CIDRS: ${TRUSTED_PROXY_CIDRS:-127.0.0.1/32}
    volumes:
      - ./.gemini:/app/.gemini
      - ./.claude:/app/.claude
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/local/bin/memoryops-healthcheck", "8080", "/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    restart: unless-stopped

  mcp:
    image: quazmoz/memoryops:mcp-latest
    build:
      context: .
      target: mcp-runtime
    command: ["/usr/local/bin/mcp"]
    ports:
      - "127.0.0.1:3003:3003"
    environment:
      DATABASE_URL: postgres://memoryops:memoryops@postgres:5432/memoryops
      REDIS_URL: redis://redis:6379
      QDRANT_URL: http://qdrant:6334
      MCP_PORT: "3003"
      MCP_TRANSPORT: "http"
      APP_SECRET_KEY: ${APP_SECRET_KEY}
    volumes:
      - ./.gemini:/app/.gemini
      - ./.claude:/app/.claude
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres-data:
  redis-data:
  qdrant-data:
